# Used to bypass upstream dependency conflicts (e.g. Next.js 15+ and ESLint v8 vs v9)
# during npm install. This helps both local setups and Vercel deployments succeed 
# without needing manual flags like --legacy-peer-deps.
legacy-peer-deps=true
